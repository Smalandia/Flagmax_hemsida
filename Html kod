





import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Mail, MapPin, Phone } from "lucide-react"export default function FlagmaxWebsite()     <div className="min-h-screen bg-gray-50 text-gray-800">
      {/* Hero / Landing */}
      <section className="bg-blue-900 text-white py             <section className="        <h2 c        <div className="gridexport default function FlagmaxWebsite md:grid-cols-3 gap-6">
          <Card clas            <CardContent              <h3 classNam              <p>Flaggstänger i tre delar sexport default function FlagmaxWebsite() {
om gör transport och installation enkel även på svårtillgängliga platser.</p>
            </CardContent>
          </Card>

          <Card className="rounded-2xl shadow">
            <CardContent cl                          <p>Vi tillverkar våra flaggstänger i Nässjö med hög kvalitet och noggrannhet.</p>
        
      {/* Services */}
    </CardContent>
          </Card>

          <Card className="rounded-2xl shadow">
            <Ca              <p>Flaggstänger i aluminium finns i olika längder för att passa olika behov och mi
      {/* Services */}
ljöer.</p>
            </CardContent>
          </Card>
        </div>
      </section>

      {/* About / Info */}
      <section className="bg-gray-100 py-16 px-6 text-center">
        <div className="max-w-3xl mx-auto">
          <h          <p className="mb-4">
            Flagmax är bas            Våra f            vilket gör dem enkla att transportera och montera – perfekt för platser d            traditionella flaggstänger är svåra att leverera.
          </p>
          <p>
            Företaget drivs lo          <Card clakalt i Näss            Genom eg          </p>
        </sec
      {/* M      <section className="py-16 px-6 bg-white">
        <h2 className="text-3xl font-semibold text-center mb-10">Hitta oss</h2>
        <div className="max-w-5xl mx-auto grid md:grid-cols-2 gap-8 items-center">
          <div>
            <p className="flex items-center gap-2 mb-2"><MapPin size={18}/> Storgatan 1, 571 31 Nässjö</p>
            <p>Vi finns i Nässjö där all tillverkning sker lokalt.</p>
          </div>
          https://www.google.com/maps?q=Nässjö&output=embed
        </div>
      </section>


      {/* Contact */}
      <section className="bg-white py-16 px-6">
        <h2 className="text-3xl font-semibold text-center mb-10">Kontakta oss</h2>
        <div className="max-w-4xl mx-auto grid md:grid-cols-2 gap-8">
          <Card className="rounded-2xl shadow">
            <CardContent className="p-6 space-y-4">
              <p className="flex items-center gap-2"><Phone size={18}/> 070-000 00 00</p>
              <p className="flex items-center gap-2"><Mail size={18}/> info@flagmax.se</p>
              <p className="flex items-center gap-2"><MapPin size={18}/> Nässjö, Sverige</p>
            </CardContent>
          </Card>

          <Card className="rounded-2xl shadow">
            <CardContent className="p-6">
              <form className="space-y-4">
                     <input className="w-full border p-2 rounded" placeholder="E-post" />
                <textarea className="w-full border p-2 rounded" placeholder="Meddelande" rows={4}></textarea>
                <Button className="w-full">Skicka</Button>
              </form>
                     </
      {/* About / Info */}
Card>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-blue-900 text-white py-6 text-center">
        <p>Flagmax • Nässjö</p>
        <p>Adress: Storgatan 1, 571 31 Nässjö</p>
      </footer>
    </div>
  );
}
